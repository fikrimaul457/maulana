#include <iostream>
using namespace std;

int main() {
  int nilai[10], batas, cari, hasil;
  cout << " masukkan banyaknya bilangan = ";
  cin >> batas;
  cout << endl << endl;
  for (int u = 0; u < batas; u++) {
    cout << " masukkan index ke - " << u << " = ";
    cin >> nilai[u];
  }
  cout << endl << endl;
  cout << " deretan bilangan = ";
  for (int e = 0; e < batas; e++) {
    cout << nilai[e] << " ";
    }
      cout << endl << " masukkan bilangan yang akan dicari = ";
      cin >> cari;
      for (int g=0; g<10; g++){
      if (nilai[g] == cari){
cout << " Bilangan "<< cari <<" ditemukan sebanyak 1 \n pada posisi ke = " << g;
break;
        }else if(cari != 1 && cari != 4 && cari != 10 && cari != 6&&cari != 3){
        cout << " Bilangan tidak ditemukan ";
        break;
        }
        }
    }
